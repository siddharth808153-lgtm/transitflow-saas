{
  "buildCommand": "cd transport-frontend && npm install && npm run build",
  "outputDirectory": "transport-frontend/build",
  "installCommand": "echo 'skipping root install'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
